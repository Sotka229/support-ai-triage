# Контекст сборки должен содержать только исходники: .git тут весит больше всего
# остального вместе взятого, а var/ — это артефакт прогона демо, не код.
.git
.gitignore
var/
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
*.egg-info/
